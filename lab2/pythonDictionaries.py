# Dictionaries are key-value pair collections, where each key maps to a corresponding value. Keys must be unique and immutable (e.g., strings, numbers), but values can be any data type.
# reminds me of map in c++
my_dict = {'name': 'Alice', 'age': 25}
print(my_dict['name'])  # Outputs: Alice
